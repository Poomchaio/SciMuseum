# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SciMuseumCU' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'LinearProgressBar'
  pod 'Apollo'
  pod 'RealmSwift'
  pod 'moa', '~> 9.0'
  pod 'Auk', '~> 8.0'
  pod 'SDWebImage/WebP' 
  # Pods for SciMuseumCU

  target 'SciMuseumCUTests' do
    inherit! :search_paths
    # Pods for testing
  inherit! :search_paths
  end

  target 'SciMuseumCUUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
